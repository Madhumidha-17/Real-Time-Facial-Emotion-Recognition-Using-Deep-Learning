# ══════════════════════════════════════════════════════════════
#  Real-Time Emotion Detection System — Python Dependencies
# ══════════════════════════════════════════════════════════════
#
#  Install all at once:
#    pip install -r requirements.txt
#
#  Python version: 3.9 – 3.11 recommended
# ══════════════════════════════════════════════════════════════

# ── Core vision & ML ──────────────────────────────────────────
opencv-python>=4.8.0         # webcam capture + face detection
numpy>=1.24.0                # array operations

# ── Deep Learning backend  (choose ONE — both listed for pip) ─
tensorflow>=2.13.0           # CNN training & inference (Keras API)
# torch>=2.0.0               # PyTorch alternative (uncomment to use)
# torchvision>=0.15.0        # PyTorch datasets & transforms

# ── Training utilities ────────────────────────────────────────
pandas>=2.0.0                # parse FER-2013 CSV
matplotlib>=3.7.0            # training history plots
scikit-learn>=1.3.0          # confusion matrix, metrics

# ── Optional: use pre-trained models from DeepFace ────────────
# deepface>=0.0.90           # uncomment to enable download_pretrained.py Option A

# ── Optional: IoT / ThingSpeak ────────────────────────────────
# requests>=2.31.0           # alternative HTTP client for IoT
